import { ITheme } from "./interfaces/theme.interface";

const theme: ITheme = {
	colors: {
		primary: "#fff",
	},
	styles: {
		boxShadow: "0 2px 6px 6px rgba(0, 0, 0, 0.1)",
		borderRadius: "3px",
		transitionTime: "300ms",
	},
};

export { theme };
